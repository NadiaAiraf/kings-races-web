---
phase: 02-core-event-flow
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/components/races/RaceListView.tsx
  - src/components/races/RaceCard.tsx
  - src/components/races/RoundHeader.tsx
  - src/components/layout/AppShell.tsx
autonomous: true
requirements: [RACE-03]

must_haves:
  truths:
    - "Race list displays all Round 1 matchups in correct order with team names resolved"
    - "Each race card shows 'Team A vs Team B' with actual team names, not slot numbers"
    - "Round headers visually separate rounds"
    - "Round 2 section shows as greyed-out placeholder for 8+ team events"
    - "When fewer than 4 teams are entered, an empty state is shown"
  artifacts:
    - path: "src/components/races/RaceListView.tsx"
      provides: "Race list container with round sections"
      exports: ["RaceListView"]
    - path: "src/components/races/RaceCard.tsx"
      provides: "Single race matchup display"
      exports: ["RaceCard"]
    - path: "src/components/races/RoundHeader.tsx"
      provides: "Round section divider"
      exports: ["RoundHeader"]
  key_links:
    - from: "src/components/races/RaceListView.tsx"
      to: "src/hooks/useDisciplineState.ts"
      via: "useDisciplineState for teams, scores, structure"
      pattern: "useDisciplineState"
    - from: "src/components/races/RaceCard.tsx"
      to: "src/domain/types.ts"
      via: "Team type for name resolution"
      pattern: "Team"
    - from: "src/components/layout/AppShell.tsx"
      to: "src/components/races/RaceListView.tsx"
      via: "conditional render on races sub-tab"
      pattern: "RaceListView"
---

<objective>
Build the race list view showing all matchups in correct order with team names resolved from slot numbers. Handles round headers, scored/unscored states, and the Round 2 greyed-out placeholder.

Purpose: Officials need to see the complete race schedule to know who races next and track progress through the event.
Output: Functional race list view integrated into the app shell's Races sub-tab.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-event-flow/02-CONTEXT.md
@.planning/phases/02-core-event-flow/02-RESEARCH.md
@.planning/phases/02-core-event-flow/02-UI-SPEC.md
@.planning/phases/02-core-event-flow/02-01-SUMMARY.md

<interfaces>
From src/hooks/useDisciplineState.ts:
```typescript
export function useDisciplineState(discipline: DisciplineKey): {
  teams: Team[];
  teamCount: number;
  scores: Score[];
  phase: DisciplineState['phase'];
  structure: TournamentStructure | null;
  manualTiebreaks: Record<string, number[]>;
};
```

From src/domain/types.ts:
```typescript
export type DisciplineKey = 'mixed' | 'board' | 'ladies';
export interface Team { slot: number; name: string; }
export interface RaceMatchup { raceNum: number; homeSlot: number; awaySlot: number; }
export interface Score {
  raceId: string; homeSlot: number; awaySlot: number;
  homeOutcome: RaceOutcome; awayOutcome: RaceOutcome;
}
export interface TournamentStructure {
  teamCount: number; groups: GroupDefinition[];
  roundOneRaces: RaceMatchup[]; roundTwoGroups?: RoundTwoGroupDefinition[];
  finals: FinalsMatchup[];
  roundOneRaceCount: number; roundTwoRaceCount?: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build RaceCard, RoundHeader, and RaceListView components</name>
  <files>
    src/components/races/RaceCard.tsx,
    src/components/races/RoundHeader.tsx,
    src/components/races/RaceListView.tsx
  </files>
  <read_first>
    - src/domain/types.ts
    - src/hooks/useDisciplineState.ts
    - .planning/phases/02-core-event-flow/02-UI-SPEC.md (Race List View section, Copywriting Contract, Interaction Contract for Undo/Correct)
    - .planning/phases/02-core-event-flow/02-CONTEXT.md (D-08)
    - .planning/phases/02-core-event-flow/02-RESEARCH.md (open question 1 about Round 2 display)
  </read_first>
  <action>
Create `src/components/races/` directory.

**RoundHeader.tsx** (per UI-SPEC Race List View):
- Props: `label: string`, `isActive: boolean`
- Render: section divider with `text-xl font-semibold leading-tight` (heading style from UI-SPEC Typography)
- Active: `text-slate-900`
- Inactive/greyed: `text-slate-400 opacity-40`
- Bottom margin: `mb-3`
- Top margin: `mt-6` (except first round, handle in parent)

**RaceCard.tsx** (per UI-SPEC Race List View):
- Props:
  ```typescript
  interface RaceCardProps {
    raceNum: number;
    homeTeamName: string;
    awayTeamName: string;
    score: Score | undefined;
    disabled?: boolean;  // for future round races
  }
  ```
- Render: card with `bg-white border border-slate-200 rounded-lg p-4`
- Race number: `text-sm text-slate-500` showing "Race {raceNum}"
- Matchup: `text-base text-slate-900` showing "{homeTeamName} vs {awayTeamName}"
- If `score` exists (scored race):
  - Show colored outcome indicators next to each team name
  - Win: small `bg-green-100 text-green-800 text-xs px-2 py-0.5 rounded` badge showing "W"
  - Loss: `bg-amber-100 text-amber-800` badge showing "L"
  - DSQ: `bg-red-100 text-red-800` badge showing "DSQ"
  - Show "Edit" link: `text-sm text-slate-500 min-h-14 min-w-14` tap target, `cursor-pointer` (per UI-SPEC Interaction Contract Undo/Correct). This link is informational only in the Race List view -- it tells officials they can re-score in the Score tab. For now just render it as a label; actual edit wiring happens in Plan 04.
- If `disabled`: `opacity-40 pointer-events-none`
- Bottom margin between cards: `mb-2`

**RaceListView.tsx** (per D-08, RACE-03):
- Props: `discipline: DisciplineKey`
- Use `useDisciplineState(discipline)` to get `teams`, `scores`, `structure`
- Build team lookup map: `const teamMap = new Map(teams.map(t => [t.slot, t.name]))`
- Build score lookup map: `const scoreMap = new Map(scores.map(s => [s.raceId, s]))`

If no structure (teamCount < 4):
- Render empty state: "Add teams first" heading, "Enter at least 4 teams to see the race schedule." body (exact copy from UI-SPEC Copywriting Contract)
- Heading: `text-lg font-semibold text-slate-900`, body: `text-sm text-slate-500`
- Center in container: `flex flex-col items-center justify-center py-12`

If structure exists:
- Render "Round 1" header with `isActive={true}`
- Map `structure.roundOneRaces` to RaceCard components:
  - `homeTeamName = teamMap.get(race.homeSlot) ?? 'Team ' + race.homeSlot`
  - `awayTeamName = teamMap.get(race.awaySlot) ?? 'Team ' + race.awaySlot`
  - `score = scoreMap.get('r1-' + race.raceNum)` (race ID convention from RESEARCH Pattern 4)
  - `key={'r1-' + race.raceNum}`

- If `structure.roundTwoGroups` exists (8+ team events, per D-08):
  - Render "Round 2" header with `isActive={false}` (greyed out)
  - Render a placeholder paragraph: `text-sm text-slate-400 opacity-40 px-4 py-2` showing "Round 2 -- {structure.roundTwoRaceCount} races, seeded from Round 1 results" (per RESEARCH recommendation for open question 1)
  - Do NOT render individual Round 2 race cards (team names can't be resolved until Round 1 completes)

- If `structure.finals` exists:
  - Render "Finals" header with `isActive={false}` (greyed out)
  - Render placeholder: "Finals seeded from group stage results" in same greyed style
  </action>
  <verify>
    <automated>cd /Users/aidanfaria/kings_races_web && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/races/RaceCard.tsx exports `RaceCard`
    - RaceCard.tsx contains "vs" text for matchup display
    - RaceCard.tsx contains `border-slate-200` and `rounded-lg`
    - RaceCard.tsx contains outcome badge classes `bg-green-100` and `bg-amber-100` and `bg-red-100`
    - src/components/races/RoundHeader.tsx exports `RoundHeader`
    - RoundHeader.tsx contains `opacity-40` for greyed-out state
    - src/components/races/RaceListView.tsx exports `RaceListView`
    - RaceListView.tsx contains `useDisciplineState`
    - RaceListView.tsx contains "Add teams first"
    - RaceListView.tsx contains "Enter at least 4 teams to see the race schedule."
    - RaceListView.tsx contains `r1-` race ID pattern
    - RaceListView.tsx contains "Round 2" text
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>Race list displays all Round 1 matchups with team names resolved from slot numbers. Scored races show outcome badges. Round 2 and Finals show as greyed-out placeholders for 8+ team events. Empty state shown when fewer than 4 teams entered.</done>
</task>

<task type="auto">
  <name>Task 2: Wire RaceListView into AppShell</name>
  <files>
    src/components/layout/AppShell.tsx
  </files>
  <read_first>
    - src/components/layout/AppShell.tsx
    - src/components/races/RaceListView.tsx
    - src/hooks/useDisciplineState.ts
    - src/hooks/useCurrentRace.ts
  </read_first>
  <action>
Update `AppShell.tsx`:

1. Import `RaceListView` from `../races/RaceListView`
2. Replace the Races placeholder with: `{activeSubTab === 'races' && <RaceListView discipline={activeDiscipline} />}`
3. Update the ProgressBar to show real data:
   - Import `useCurrentRace` from `../../hooks/useCurrentRace`
   - Call `const { currentIndex, totalRaces } = useCurrentRace(activeDiscipline);`
   - Pass to ProgressBar: `<ProgressBar current={currentIndex + 1} total={totalRaces} />`
   - When `totalRaces === 0`, ProgressBar already handles showing "No races yet"

This wires the race list into the Races sub-tab and connects the progress bar to real race data.
  </action>
  <verify>
    <automated>cd /Users/aidanfaria/kings_races_web && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - AppShell.tsx imports `RaceListView`
    - AppShell.tsx contains `activeSubTab === 'races'`
    - AppShell.tsx imports `useCurrentRace`
    - AppShell.tsx passes real values to ProgressBar (not hardcoded 0)
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>Race list view is accessible via the Races sub-tab. Progress bar shows real race progress based on scored races.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. `npm run build` succeeds
3. With fewer than 4 teams, Races tab shows "Add teams first" empty state
4. With 4+ teams, Races tab shows numbered race cards with "Team A vs Team B"
5. For 8+ teams, Round 2 and Finals placeholders are visible but greyed out
6. Progress bar updates to show "Race N of M"
</verification>

<success_criteria>
- Race list displays all matchups in correct order showing "Team A vs Team B" with team names resolved (RACE-03)
- Round headers separate Round 1, Round 2, and Finals sections
- Round 2 and Finals are greyed out but visible for 8+ team events (D-08)
- Empty state shown when insufficient teams
- Progress bar reflects actual race progress
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-event-flow/02-03-SUMMARY.md`
</output>
