---
phase: 09-seeding-r2-order-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/types.ts
  - src/domain/cheatSheets/teams4.ts
  - src/domain/cheatSheets/teams5.ts
  - src/domain/cheatSheets/teams6.ts
  - src/domain/cheatSheets/teams7.ts
  - src/domain/cheatSheets/teams8.ts
  - src/domain/cheatSheets/teams9.ts
  - src/domain/cheatSheets/teams10.ts
  - src/domain/cheatSheets/teams11.ts
  - src/domain/cheatSheets/teams12.ts
  - src/domain/cheatSheets/teams13.ts
  - src/domain/cheatSheets/teams14.ts
  - src/domain/cheatSheets/teams15.ts
  - src/domain/cheatSheets/teams16.ts
  - src/domain/cheatSheets/teams17.ts
  - src/domain/cheatSheets/teams18.ts
  - src/domain/cheatSheets/teams19.ts
  - src/domain/cheatSheets/teams20.ts
  - src/domain/cheatSheets/teams21.ts
  - src/domain/cheatSheets/teams22.ts
  - src/domain/cheatSheets/teams23.ts
  - src/domain/cheatSheets/teams24.ts
  - src/domain/cheatSheets/teams25.ts
  - src/domain/cheatSheets/teams26.ts
  - src/domain/cheatSheets/teams27.ts
  - src/domain/cheatSheets/teams28.ts
  - src/domain/cheatSheets/teams29.ts
  - src/domain/cheatSheets/teams30.ts
  - src/domain/cheatSheets/teams31.ts
  - src/domain/cheatSheets/teams32.ts
autonomous: true
requirements:
  - SEED-02
  - R2ORD-01

must_haves:
  truths:
    - "Every TournamentStructure includes a seedMap array matching the golden data"
    - "R2 race order for all applicable team counts matches the xlsx when flattened"
    - "All 288 structural integrity tests continue to pass"
  artifacts:
    - path: "src/domain/types.ts"
      provides: "seedMap field on TournamentStructure"
      contains: "seedMap: number[]"
    - path: "src/domain/cheatSheets/teams8.ts"
      provides: "Representative cheat sheet with seedMap and corrected R2 races"
      contains: "seedMap:"
  key_links:
    - from: "src/domain/cheatSheets/teams*.ts"
      to: "src/domain/types.ts"
      via: "TournamentStructure type with required seedMap"
      pattern: "seedMap:"
    - from: "src/domain/cheatSheets/__fixtures__/goldenData.json"
      to: "src/domain/cheatSheets/teams*.ts"
      via: "Golden data values transcribed into TS files"
      pattern: "seedMap.*\\["
---

<objective>
Add seedMap arrays and fix R2 race orders in all 29 cheat sheet files, driven by the golden data fixture.

Purpose: Establish the correct seed-to-slot mapping data and R2 race sequences so that serpentine seeding and R2 ordering match the xlsx source of truth. This is the data foundation that Plan 02 will wire into the assignSlots domain function.

Output: Updated types.ts with seedMap field; all 29 cheat sheet files with seedMap arrays; 26 cheat sheet files with corrected R2 race arrays; all 26 previously-failing R2 validation tests now passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-seeding-r2-order-fix/09-CONTEXT.md
@.planning/phases/09-seeding-r2-order-fix/09-RESEARCH.md

@src/domain/types.ts
@src/domain/cheatSheets/__fixtures__/goldenData.json
@src/domain/cheatSheets/index.ts
@src/domain/cheatSheets/teams8.ts (representative structure)
@src/domain/cheatSheets/goldenDataValidation.test.ts

<interfaces>
<!-- Key types the executor needs -->

From src/domain/types.ts:
```typescript
export interface TournamentStructure {
  teamCount: number;
  // seedMap: number[] will be added HERE
  groups: GroupDefinition[];
  roundOneRaces: RaceMatchup[];
  roundTwoGroups?: RoundTwoGroupDefinition[];
  finals: FinalsMatchup[];
  roundOneRaceCount: number;
  roundTwoRaceCount?: number;
}

export interface RoundTwoGroupDefinition {
  groupNum: string;
  seedingEntries: RoundTwoSeeding[];
  races: RoundTwoRace[];
}

export interface RoundTwoRace {
  raceNum: number;
  homeLetter: string;
  awayLetter: string;
}
```

Golden data structure (goldenData.json):
```json
{
  "8": {
    "teamCount": 8,
    "seedMap": [1, 11, 14, 4, 2, 12, 13, 3],
    "r1Races": [{ "raceNum": 1, "homeSlot": 1, "awaySlot": 2 }, ...],
    "r2Races": [{ "raceNum": 1, "homeLetter": "A", "awayLetter": "B" }, ...]
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add seedMap to TournamentStructure type and update all 29 cheat sheet files</name>
  <files>
    src/domain/types.ts,
    src/domain/cheatSheets/teams4.ts through teams32.ts (29 files)
  </files>
  <read_first>
    src/domain/types.ts,
    src/domain/cheatSheets/__fixtures__/goldenData.json,
    src/domain/cheatSheets/teams8.ts,
    src/domain/cheatSheets/teams4.ts,
    src/domain/cheatSheets/teams12.ts
  </read_first>
  <action>
    **Step 1 — Type change (per D-01):**
    In `src/domain/types.ts`, add `seedMap: number[];` to the `TournamentStructure` interface immediately after the `teamCount: number;` line. Make it REQUIRED (not optional). This will cause TypeScript errors in all 29 cheat sheet files, which is the desired enforcement.

    **Step 2 — Script-driven cheat sheet updates (per D-02, D-03):**
    Write a temporary Node.js script (e.g., `scripts/updateCheatSheets.mjs`) that:

    1. Reads `src/domain/cheatSheets/__fixtures__/goldenData.json`
    2. For each team count 4-32, reads the corresponding `src/domain/cheatSheets/teams{N}.ts` file
    3. Inserts `seedMap: [...]` on the line after `teamCount: N,` using the golden data's seedMap array
    4. For team counts with `roundTwoGroups` (7-32), reconstructs per-group R2 race arrays from the golden flat R2 sequence:
       - Reads each R2 group's letters from the existing `seedingEntries[].letter` values in the TS file
       - Builds a letter-to-group-index mapping
       - Filters the golden flat R2 races by which group each race belongs to (both homeLetter and awayLetter belong to the same group)
       - Replaces each group's `races: [...]` array with the filtered+ordered races, renumbering raceNum sequentially within each group (starting at 1)
    5. Writes the updated file back

    **CRITICAL: R2 race reconstruction logic:**
    The golden data `r2Races` is a flat interleaved sequence (round-by-round across groups). To reconstruct per-group arrays:
    - For each race in the golden flat list, look up `homeLetter` in the letter-to-group mapping to determine which R2 group it belongs to
    - Append the race to that group's array
    - After all races are distributed, renumber each group's races sequentially (raceNum 1, 2, 3...)
    - The validation test flattens Group I races + Group II races + ... and compares to golden flat, so group-internal ordering must preserve the golden interleaving order

    **Step 3 — Run the script:**
    Execute the script with `node scripts/updateCheatSheets.mjs`. Verify it completes without errors.

    **Step 4 — Verify TypeScript compiles:**
    Run `npx tsc --noEmit` to confirm all 29 files now satisfy the required `seedMap` field.

    **Step 5 — Clean up:**
    Delete the temporary script after successful updates.

    **Do NOT:**
    - Make seedMap optional (`seedMap?: number[]`) — it must be required per D-01
    - Manually edit cheat sheet files — use the script for reliability across 29 files
    - Change any R1 race data, groups, seedingEntries, or finals structures
    - Modify roundTwoRaceCount values (they already match golden data per research)
  </action>
  <verify>
    <automated>cd /Users/aidanfaria/kings_races_web && npx tsc --noEmit && npx vitest run src/domain/cheatSheets/cheatSheets.test.ts src/domain/cheatSheets/goldenDataValidation.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "seedMap: number\[\]" src/domain/types.ts returns 1
    - grep -l "seedMap:" src/domain/cheatSheets/teams*.ts returns 29 files
    - npx tsc --noEmit exits 0 (all 29 files have seedMap)
    - npx vitest run src/domain/cheatSheets/goldenDataValidation.test.ts shows 87 tests passing (0 failing)
    - npx vitest run src/domain/cheatSheets/cheatSheets.test.ts shows 288 tests passing (no regressions)
  </acceptance_criteria>
  <done>All 29 cheat sheet files contain seedMap arrays matching golden data. All 26 previously-failing R2 race order tests now pass. All 288 structural integrity tests continue to pass. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — TypeScript compiles with required seedMap on all 29 files
2. `npx vitest run src/domain/cheatSheets/goldenDataValidation.test.ts` — All 87 golden data validation tests pass (VALID-01, VALID-02, VALID-03)
3. `npx vitest run src/domain/cheatSheets/cheatSheets.test.ts` — All 288 structural tests pass
4. `grep -c "seedMap:" src/domain/cheatSheets/teams*.ts | grep -v ":1$"` — Should return nothing (every file has exactly one seedMap)
</verification>

<success_criteria>
- seedMap field exists as required on TournamentStructure type
- All 29 cheat sheet files contain seedMap arrays matching golden data exactly
- 26 R2 race arrays corrected to match golden data interleaved order
- All 87 golden data validation tests pass (was: 26 failing)
- All 288 structural integrity tests pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/09-seeding-r2-order-fix/09-01-SUMMARY.md`
</output>
